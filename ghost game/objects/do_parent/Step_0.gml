/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

/*
var obj = collision_circle(x,y,sqrt(width^2 + height^2),o_ghost,false,true);
if (obj != noone)
{
	if (C.trigRight)
	{
		var xforce = darctan2(y-obj.y,x-obj.x);
		physics_apply_force(x,y,
	}
}